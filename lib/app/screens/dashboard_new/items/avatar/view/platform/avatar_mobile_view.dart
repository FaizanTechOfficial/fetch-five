import 'package:fetch_five/app/screens/dashboard_new/items/avatar/controller/avatar_controller.dart';
import 'package:flutter/material.dart';

class AvatarMobileView extends StatelessWidget {
  const AvatarMobileView({super.key, required this.avatarController});
  final AvatarController avatarController;

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
